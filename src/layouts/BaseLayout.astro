---
import '../styles/global.css';
import { withBase } from '../lib/site';

interface Props {
  title: string;
  description: string;
  noindex?: boolean;
  schema?: Record<string, unknown> | Record<string, unknown>[];
  image?: string;
}

const {
  title,
  description,
  noindex = false,
  schema,
  image = '/images/generated/yachenyi-brand-visual.webp',
} = Astro.props;

const canonical = Astro.site ? new URL(Astro.url.pathname, Astro.site) : Astro.url;
const socialImage = Astro.site ? new URL(withBase(image), Astro.site) : withBase(image);
const path = Astro.url.pathname;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const logicalPath = base && path.startsWith(base) ? (path.slice(base.length) || '/') : path;
const isCurrent = (prefix: string) =>
  logicalPath === prefix || (prefix !== '/' && logicalPath.startsWith(prefix));
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#f7f7f4" />
    <link rel="icon" type="image/svg+xml" href={withBase('/favicon.svg')} />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex,nofollow" />}
    <link rel="canonical" href={canonical} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Yachenyi" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />

    {Astro.site && <link rel="sitemap" href={withBase('/sitemap-index.xml')} />}
    {schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to content</a>

    <header class="site-header">
      <a class="brand" href={withBase('/')} aria-label="Yachenyi home">YACHENYI<span class="brand-dot">.</span></a>
      <nav aria-label="Primary">
        <a href={withBase('/projects/')} aria-current={isCurrent('/projects/') ? 'page' : undefined}>Projects</a>
        <a href={withBase('/services/')} aria-current={isCurrent('/services/') ? 'page' : undefined}>Services</a>
        <a href={withBase('/lab/')} aria-current={isCurrent('/lab/') ? 'page' : undefined}>Lab</a>
        <a href={withBase('/about/')} aria-current={isCurrent('/about/') ? 'page' : undefined}>About</a>
      </nav>
      <a class="header-contact" href={withBase('/contact/')} aria-current={isCurrent('/contact/') ? 'page' : undefined}>Contact ↗</a>
    </header>

    <main id="content">
      <slot />
    </main>

    <footer class="site-footer">
      <div>
        <strong>YACHENYI</strong>
        <p>Independent AI Software Studio</p>
      </div>
      <div class="footer-links">
        <a href={withBase('/projects/')}>Projects</a>
        <a href={withBase('/lab/')}>Lab</a>
        <a href={withBase('/facts/')}>Facts</a>
        <a href={withBase('/contact/')}>Contact</a>
      </div>
      <p class="footer-meta">One person. AI-native. Software first.</p>
    </footer>
  </body>
</html>
